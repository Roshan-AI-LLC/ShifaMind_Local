# benchmark/requirements.txt
# Additional dependencies for the ShifaMind benchmark.
# The base ShifaMind requirements.txt must be installed first.

# ── LLM API clients ───────────────────────────────────────────────────────────
openai>=1.12.0
anthropic>=0.21.0
google-generativeai>=0.5.0

# ── Config ────────────────────────────────────────────────────────────────────
pyyaml>=6.0

# ── Statistics (bootstrap CI, McNemar) ───────────────────────────────────────
scipy>=1.12.0   # already in base requirements, listed for clarity
